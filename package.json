{
  "name": "ionic-stenciljs-layout",
  "private": true,
  "version": "0.0.1",
  "description": "ionic-stenciljs-layout",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "prerender": "stencil build --prerender",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate",
    "serve": "serve www"
  },
  "devDependencies": {
    "@ionic/core": "^5.6.3",
    "@stencil/core": "^2.5.1",
    "serve": "^11.3.2"
  },
  "engines": {
    "node": "12.x.x"
  }
}
